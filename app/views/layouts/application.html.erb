<!DOCTYPE html>
<html>
<head>
  <title>To The Trails Administration</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <div class="topheader">
    <h4>To The Trails Admin</h4>
    <ul class="hmenu" id="logInText-email"> 
      <%= render 'devise/menu/registration_items' %>
      <%= render 'devise/menu/login_items' %>
    </ul>
  </div>
  <div class="menubox">
    <ul class="hmenu">
      <li class="test <%= 'active' if current_page?(trails_path) %>"><%= link_to "Trails", trails_path %></li>
      <li class="<%= 'active' if current_page?(trailheads_path) %>"><%= link_to "Trailheads", trailheads_path %></li>
      <li class="<%= 'active' if current_page?(trailsegments_path) %>"><%= link_to "Segments", trailsegments_path %></li>
      <li class="<%= 'active' if current_page?(organizations_path) %>"><%= link_to "Organizations", organizations_path %></li>
      <% if current_user && current_user.admin? %>
        <li class="<%= 'active' if current_page?(users_path) %>"><%= link_to "Users", users_path %></li>
      <% end %>
    </ul>
  </div>  
<%= yield %>

</body>
</html>
