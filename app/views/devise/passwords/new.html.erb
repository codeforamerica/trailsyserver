<h1>Forgot your password?</h1>
<div call='formContent'>
	<p class='uploadDescription'>
	Enter your email address and we'll send you a link to reset your password. We should also add email of admin for issues?
	</p>
	<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
	<%= devise_error_messages! %>
	<div>
	  	<span class='h3-Edit'><%= f.label :email %></span>
	  	<%= f.email_field :email, :autofocus => true %></div>
	<br>
	<div><%= f.submit "Send Email" %></div>
	<% end %>

</div>
<div class='formButtons'>
<%= render "devise/shared/links" %>
</div>
