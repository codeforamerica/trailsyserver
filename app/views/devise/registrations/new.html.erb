<h1>Sign Up</h1>

  <p class='uploadDescription'>
    Want your park data on To The Trail for Summit County?
  </p>
  <p class='uploadDescription'>
    Contact the administor at email@email.com. Once the administrator sets up an account for you, come back to this page and sign up to set your email and a password. The adminitrator will verfity your email and approve your account. Next step: add your data!
  </p>
  
<div class='formContent'>
  <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>

    <span class='h3-Edit'><%= f.label :email %></span>
    <%= f.email_field :email, :autofocus => true %>

    <div>
      <span class='h3-Edit'><%= f.label :organization %></span>
      <%= f.select :organization_id, Organization.all.collect{ |org| [org.code, org.id]} %>
      <br>
      <span class='finePrint'>If your organization is not listed here, contact an administrator.
        <br>
      This "source" field in the trail data files you will supply must contain this code.</span>
    </div>
    <div>
      <span class='h3-Edit'><%= f.label :password %></span>
      <span class='finePrint'>Your password needs to be 8 characters long.</span>
      <%= f.password_field :password %></div>

    <div>
      <span class='h3-Edit'>
        <%= f.label :password_confirmation %></span>
      <%= f.password_field :password_confirmation %></div>

    <br>
    <div><%= f.submit "Sign Up" %></div>
  <% end %>
</div>

<div class='formButtons'>
  <%= render "devise/shared/links" %>
</div>
